// Exemplo de top-level statements
writeLine("Hello, World!")

val a = 10
val b = 20
writeLine(a + b)

if (a > b) {
  writeLine("a is greater than b")
} else {
  writeLine("b is greater than or equal to a")
}